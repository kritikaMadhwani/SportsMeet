<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.SportsMeet" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.SportsMeet.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_system_placeholder</item>
        <item name="postSplashScreenTheme">@style/Theme.SportsMeet</item>
    </style>
</resources>
